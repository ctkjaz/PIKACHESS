name: pika_chess
description: A Flutter chess game with Pikachu pawns!
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_chess_board: ^1.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/pikachu.png
